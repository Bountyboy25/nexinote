// ─────────────────────────────────────────────────────────────
// TYPES — Phase 2: discriminated union cards + connectors
//
// TypeScript narrows based on card.type so each card variant
// has a fully-typed content object. Add a new card type by:
//   1. Define its interface below
//   2. Add to the Card union
//   3. Add a case to the CardFactory switch in CardNode
// ─────────────────────────────────────────────────────────────
export {};
